# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/

# Local environment
.env
.env.*

# Editors and OS
.DS_Store
.idea/
.vscode/

# Build artifacts
build/
dist/
*.egg-info/

# Project data
# Current repository mode: keep the reference MODOM workbook and processed
# artifacts versioned so the project can be continued locally after pull.

# Temporary outputs
outputs/
tmp/
logs/

# Generated dashboard (rebuilt with scripts/build_dashboard.py; not versioned to
# avoid merge conflicts on this large, regenerable artifact)
results/dashboard/seni_dashboard.html

# DIgSILENT native project (binary, ~23MB - not versioned)
*.pfd

# corridas y proyectos generados por la plataforma
results/runs/
results/projects/
results/pypsa_milp/scenarios/
results/pypsa_milp/overrides.json
